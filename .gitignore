# Dependencies
node_modules/
.pnp
.pnp.js

# Next.js
.next/
out/

# Build
dist/
build/

# Uploads (SCORM, media, mailings)
public/uploads/
uploads/

# Env (секреты не коммитить)
.env
scripts/.deploy.env
.telegram-proxy/
.env.local
.env.development.local
.env.test.local
.env.production.local
.env*.local

# Debug & logs
*.log
npm-debug.log*

# OS
.DS_Store
Thumbs.db

# Playwright
tests/.auth/
test-results/
playwright-report/
playwright/.cache/

# SQLite dev DB (локальная копия)
prisma/dev.db
# Бэкапы БД (сервер/ручные копии — не коммитить)
prisma/dev.db.bak*
prisma/*.db.bak*

# IDE
.idea/
# .vscode — в репозитории (tasks/deploy); локальные оверрайды — в *.local.json (игнор ниже)
.vscode/*.local.json
*.swp
*.swo

# Windows WSL metadata
*:Zone.Identifier
*.Zone.Identifier

# Supabase CLI кеш
supabase/.temp/

# Служебные выводы скриптов (не версионировать)
scripts/ps-out.txt
scripts/ss-result.txt
scripts/bulk-replace-log.txt

# Личные заметки в корне репозитория
/ЗАПУСК-ДЕМО-ДЛЯ-ДРУГА.cmd
/КАК-ПОКАЗАТЬ-САЙТ-ДРУГУ.txt
