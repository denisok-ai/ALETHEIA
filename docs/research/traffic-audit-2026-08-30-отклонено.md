# Отклонённое в ходе аудита (2026-08-30)

Раздел важен, чтобы следующий не повторил путь.

1. **«Sitemap отдаёт 503 ИИ-агентам» — ЛОЖНАЯ ТРЕВОГА МОЕГО ЖЕ ПАЙПЛАЙНА.**
   Первый замер показал «17 × /sitemap.xml» в списке 503-путей, но фильтр по
   коду в той команде не сработал (экранирование awk в ssh): список был просто
   топом путей ИИ-агентов без фильтра. Точная проверка `grep " /sitemap.xml" +
   awk $9==503` дала **ноль**; sitemap 126×200. Урок: перепроверять собственные
   пайплайны так же состязательно, как чужие данные.

2. **«ChatGPT-User активно читает сайт» — наполовину.** Больша́я часть хитов
   с этим UA — сканер уязвимостей, маскирующийся под агента (пути /.env,
   /credentials.json, SSRF на 169.254.169.254, всё 404). Настоящие фетчи есть,
   но их сотни, а не тысячи. Урок: UA без проверки путей/кодов — не факт.

3. **«ИИ-агентов блокирует fail2ban/rate-limit» — отклонено.** Jails пусты,
   limit_req в nginx не настроен. 503 — минутные окна деплоев.

4. **«site:avaterra.pro пуст ⇒ сайта нет в индексе Google» — не доказуемо
   этим инструментом.** Поисковый прокси не поддерживает оператор `site:`
   буквально (вернул домены avaterra.com/шрифты/крипту). Вывод о нулевой
   видимости сделан по другому сигналу: точная строка «avaterra.pro» не
   встречается ни на одном стороннем сайте выдачи.

5. **«Мало трафика из-за слабого контента/разметки» — отклонено.**
   42 статьи, полная JSON-LD-разметка, 0 ошибок аудита, перелинковка,
   Googlebot ходит активно. Контентная база достаточна; узкое место —
   внешние сигналы и Яндекс.
