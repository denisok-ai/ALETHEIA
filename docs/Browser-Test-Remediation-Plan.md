# План доработки по результатам браузерного тестирования

**Дата тестирования:** 15 марта 2025  
**Источник:** `docs/Browser-Test-Report-2025-03-15.md`  
**URL:** http://localhost:3001

---

## Резюме тестирования

Проведено полное тестирование под ролями: **гость**, **студент**, **менеджер**, **администратор**. Проверены публичные страницы, формы, модалки, ЛК студента, кабинет менеджера, админка, сквозные сценарии (тикет студент→менеджер, защита портала).

**Основной функционал работает.** Выявлено 1 критичная проблема, несколько замечаний и рекомендаций.

---

## Приоритеты

| Уровень | Описание |
|---------|----------|
| **Критично** | Блокирует или сильно ухудшает UX |
| **Важно** | Влияет на стабильность, навигацию, тестовые сценарии |
| **Желательно** | Улучшения, устранение предупреждений |

---

## Критичные задачи

### 1. 404 на /portal при первом редиректе после логина студента

**Статус:** Исправлено. После signIn вызывается getSession(), по роли определяется дашборд (admin/manager/student), редирект идёт сразу на `/portal/*/dashboard`, минуя `/portal`. Файл: `app/(auth)/login/page.tsx`.

---

### 2. Редирект при signOut на другой порт

**Статус:** Проверено. PortalHeader, PortalAccountBlock, signout используют `callbackUrl: window.location.origin + '/login'`. Редирект остаётся на текущем хосте/порту.

---

## Важные задачи

### 3. SCORM-контент для тестовых курсов

**Статус:** Исправлено. Seed создаёт минимальный SCORM-контент (index.html, lesson1–3.html) для course-seed-1 и course-seed-2 в `public/uploads/scorm/courses-{id}/`. После `npx prisma db seed` плеер показывает контент без загрузки ZIP. Файл: `prisma/seed.ts`.

---

### 4. Клик по карточке курса не всегда переводит на страницу курса

**Статус:** Исправлено. Overlay с play-кнопкой имел `inset-0` и перехватывал все клики. Добавлен `pointer-events-none` на overlay и `pointer-events-auto` на кнопку — клики по обложке проходят к cover div (переход на страницу курса), клики по кнопке — на плеер. Файл: `components/portal/CourseCard.tsx`.

---

## Желательные задачи

### 5. data-cursor-ref и гидратация

**Проблема:** Warning `Extra attributes from the server: data-cursor-ref` в Footer и др. Атрибут добавляется Cursor IDE.

**Действия:** В production атрибута не будет. При необходимости — условный рендер или исключение из серверного HTML. Низкий приоритет.

---

### 6. Hydration-ошибки (localhost:3000)

**Проблема:** `Hydration failed`, `Expected server HTML to contain a matching <header>`. Возникают при редиректе signOut на другой порт (3000).

**Действия:** После исправления п. 2 (callbackUrl) проверить, исчезли ли ошибки. Если остаются — проверить Header/Footer на условный рендер по `typeof window`.

---

## Чек-лист перед релизом (дополнение к Testing-Improvement-Plan)

- [x] 404 на /portal при первом редиректе — исправлено (редирект сразу на дашборд)
- [x] signOut редирект на правильный хост — проверено
- [x] Клик по карточке курса — исправлено (pointer-events)
- [x] SCORM-плеер: course-seed-1, course-seed-2 — минимальный контент в seed
- [ ] Консоль без критичных ошибок на production URL
- [ ] (Опц.) После обновления с старого seed: `npm run db:fix-verification-urls` — см. `docs/Local-Prisma.md`

---

## Проход кабинета менеджера (ручная проверка в браузере)

**Дата:** 4 апреля 2026  
**Учётка:** `manager@test.local` / `Test123!`  
**Окружение:** `http://localhost:3000`

**Обновление статуса:** апрель 2026 — см. блок «Статус реализации» ниже.

### Резюме

Основные разделы менеджера открываются: дашборд, тикеты (таблица после загрузки), верификации, пользователи, помощь. Переход в админку корректно ведёт на `/portal/access-denied?section=admin`. Ниже — доработки по данным, доступности и копирайту.

### Критично

- Отдельных блокеров не зафиксировано (страницы рендерятся, редиректы ролей ожидаемы).

### Важно

| # | Задача | Статус |
|---|--------|--------|
| 1 | **Верификации — ссылка на материал.** Якоря `/portal/...#video-N` и прочие невалидные URL: seed, UI «файл не указан», скрипт для старых БД, студенческий ЛК. | **Сделано** |
| 2 | **Фильтры верификаций — различимые имена.** Уникальные `aria-label` у списков статуса и типа. | **Сделано** |
| 3 | **Поля периода (даты).** `htmlFor`/`id` у подписей и `aria-label` у полей дат. | **Сделано** |
| 4 | **Кнопки в строке верификаций.** `aria-label` с контекстом (слушатель, курс) для Чат / OK / Нет / материал. | **Сделано** |
| 5 | **Тикеты — колонка «Открыть».** Метка «Открыть тикет: {subject}» в таблице и на дашборде менеджера. | **Сделано** |

Подробности реализации п. 1: `lib/verification-submission.ts` (`isOpenableVideoMaterialUrl`), `VerificationsTableClient.tsx`, `prisma/seed.ts` (видео → `/uploads/verifications/seed-verification-placeholder.mp4`, часть строк — текст), `npm run db:fix-verification-urls`, студент: `VerificationsPageClient.tsx`, `CourseVerificationBlock.tsx`.

### Желательно

| # | Задача | Статус |
|---|--------|--------|
| 1 | **Дашборд: большие счётчики при seed** (сотни тикетов/верификаций). | **Сделано** — при значении больше 999 на карточке «999+»; точное число в `title` и `aria-label` (`ManagerDashboardView.tsx`) |
| 2 | **Дублирование landmark навигации** (мобильное меню). | **Сделано** — разные имена: десктоп `aside` «Боковое меню портала (десктоп)», внутренний `<nav>` «Разделы портала, боковая панель»; мобильный `dialog` «Меню навигации портала (мобильное)» + `<nav>` «Разделы портала, мобильное меню» (`PortalSidebar.tsx`) |
| 3 | **Номер в теме тикета ≠ UUID в URL.** | **Сделано** — блок «ID тикета» и пояснение для менеджера в `TicketThread.tsx` |
| 4 | **ЛК студента под менеджером.** | **Сделано** — баннер «Режим просмотра как слушатель» (`StudentDashboardView` + `page.tsx`), E2E в `tests/e2e/manager/tickets.spec.ts` |
| 5 | **Помощь: «Истории» vs «История заряда».** | **Сделано** — едино «История заряда» в `HelpContent.tsx` |
| 6 | **Регрессия E2E / Excel.** | **Частично** — E2E: Excel верификаций и тикетов (`tickets.spec.ts`); полная ручная проверка файлов перед релизом — по процессу команды |

### Дополнительно сделано (сверх первоначального списка)

- **Админка — медиатека:** осмысленные `aria-label` у действий в таблице и сетке (`MediaAdminClient.tsx`).
- **Документация:** `docs/Local-Prisma.md` — команда `npm run db:fix-verification-urls`.

### Статус реализации (апрель 2026) — чек-лист

- [x] Верификации: валидность URL материала, seed, скрипт `db:fix-verification-urls`
- [x] Верификации: a11y фильтров и действий в строке
- [x] Тикеты: a11y «Открыть», подсказка UUID для менеджера
- [x] Портал: различимые aria-label десктопного сайдбара, мобильного диалога и обоих `<nav>`
- [x] Студент: баннер при просмотре ЛК менеджером/админом; тексты для битой ссылки на видео
- [x] Помощь: терминология «История заряда»
- [x] Счётчики на дашборде менеджера при «тяжёлом» seed — компактный показ «999+», точное число в подсказке и для скринридеров
- [ ] Полный прогон E2E перед релизом; ручная проверка содержимого Excel (верификации и тикеты — автотесты проверяют скачивание и имя файла)

---

## Связанные документы

- `docs/Browser-Test-Report-2025-03-15.md` — полный отчёт тестирования
- `docs/Testing-Improvement-Plan.md` — план по результатам предыдущего тестирования
- `docs/User-Journey-Roles-Audit.md` — пользовательские пути по ролям
