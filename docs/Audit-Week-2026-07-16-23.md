# Аудит доработок за неделю 16–23 июля 2026

Независимый аудит: восстановлен полный ход работ по git-истории (78 коммитов), дневнику ([Diary.md](Diary.md)), CHANGELOG и проверками на живом сервере. Прод на момент аудита: все проверки `scripts/prod-verify.ts` зелёные.

## Хронология

### Ср 16.07 — фундамент безопасности и витрина
- Защита приватной статики (SCORM/медиа через nginx `auth_request`), timing-safe сравнение секретов, экранирование JSON-LD.
- Публичная витрина услуг `/services` + `/services/[slug]` (SSR из БД), `llms.txt`/`llms-full.txt`, allow-правила для ИИ-краулеров.
- Ежедневные шифрованные бэкапы БД и настроек в Google Drive.

### Чт 17.07 — платежи и SEO-раунд 2
- Устойчивость при недоступности PayKeeper (таймауты, алерты) + проактивный мониторинг каждые 5 минут.
- Course rich results, `sameAs`, IndexNow, гео-сигнал Москва; обогащение страниц тарифов (позже дало дубли — см. 20.07).
- Deadman-страховка деплоя, `StartLimit*` systemd, обложки через `next/image`, удалено 63 МБ неиспользуемых картинок.

### Сб 19.07 — марафон надёжности (48 коммитов)
- **Безопасность/AI:** защита от prompt-инъекций, закрыт обход rate limit через `X-Forwarded-For`, CSP без `unsafe-eval`, бот перестал выдумывать тарифы и использовать несуществующий ID модели Anthropic (7 мест).
- **Деньги:** «оплачено, но доступа нет» закрыто тремя слоями (атомарный claim статуса, самолечение при повторном вебхуке, сверка-cron `reconcile-enrollments`); запрет `refunded → paid`; двойные счета рассрочки; WAL + `busy_timeout`.
- **Инцидент дня:** деплой уничтожал свежие WAL-транзакции прода (rsync затирал `dev.db-wal`) — исправлен в тот же час (`d76866d`): checkpoint перед rsync + исключение `dev.db*`.
- **Блог-платформа:** хранение в БД (`BlogPost`), админка, импорт из Telegram-канала с фото (через прокси — Telegram с прода напрямую недоступен), автопубликация раз в сутки, пагинация.

### Вс 20.07 — SEO-аудит и Яндекс
- **Главная находка недели:** `robots.txt` запекался на сборке и уезжал на прод с `Sitemap: http://localhost:3000/sitemap.xml` — поисковики ни разу не получили карту сайта. Фикс: `force-dynamic`; проверка содержимого robots добавлена в `prod-verify`.
- 301 `www → без-www` в nginx; удаление выдуманных контактов (+7 999 123-45-67, «ул. Здоровья, 10») из schema.org и правовых документов; подсказка разработчику убрана из подвала.
- Дубли тарифных страниц 86–89% → максимум 58% (блоки из фактического состава тарифа); FAQPage-разметка снята с тарифов.
- Яндекс: IndexNow подключён к публикации блога (был в проекте, но не вызывался), `Clean-param` (без `page`!), перелинковка статей цепочкой (0 сирот), кэш картинок 60 с → 30 дней.
- 10 SEO-статей из базы знаний `content/knowledge/avaterra.yaml` — от реального запроса аудитории к тексту; правила KB соблюдены (без цен, «мы не лечим», дисклеймеры).

### Вт 22.07 (Cursor) — инцидент PayKeeper и SEO/GEO
- **~15:59 инцидент:** три заказа по 25 000 ₽ за минуту не создали счёт — «Токен безопасности не верен». Причина: кэш токена на 23 ч при ротации на стороне PayKeeper раз в 24 ч по их расписанию + гонка параллельных счетов.
- Фикс в два захода: промежуточный (TTL 5 мин) честно признан костылём и заменён системным (`d469cdb`): свежий GET токена перед каждым mutating POST, in-flight dedup, сериализация POST-with-token на server+login, один retry. Код проверен — реализация корректная.
- Для пострадавших заказов: `scripts/manual-register-student.ts` (env-параметры, dry-run, без секретов) + одноразовые `_tmp-resend-course-email.ts`/`_tmp-run-resend.sh`.
- Вечером — live-аудит SEO: написан [SEO.md](SEO.md) (почему прошлые итерации дали ~0, стратегия Google AI Overviews / Яндекс / GEO, ручные шаги владельца). Fail-safe sitemap (ловили спорадические 500), entity-тексты `/about` и главной, снят корневой canonical, комментарии-ссылки на llms.txt в robots. Задеплоено и собрано 22.07 20:41 — **закоммичено только утром 23.07** (`ab3064a`).

### Ср 23.07 (Cursor) — почта @avaterra.pro
Работа целиком на сервере, в коде отсутствует (только docs):
- DKIM: селектор `dkim`, 2048 бит, прописан в Redis Mailcow (`_tmp-prod-dns-deploy.sh`).
- Alias `dmarc@avaterra.pro → admin@` для RUA-отчётов.
- DNS nic.ru: MX, SPF, DKIM, DMARC; PTR `95.181.224.70 → mail.avaterra.pro` через хостера.
- Проверка через Google DoH и авторитетные NS; welcome-письма в Gmail и Mail.ru без bounce. Статус «working» подтверждён. Итог в [Mail-Current-State.md](Mail-Current-State.md); коммит `153158c`.
- Это завершение многодневной линии (7, 13, 22–23.07) по подъёму собственного почтового сервера.

## Находки аудита (процессные, не кодовые)

1. **Прод работал на незакоммиченном коде.** Файлы `ab3064a` собраны на проде 22.07 20:41 — за 12 часов до коммита. Подиффная сверка: содержимое на проде идентично `ab3064a`, но git-дерево сервера «грязное» (9 M-файлов поверх `d469cdb`). До пуша любой `git reset --hard` уничтожил бы работающий код. Сейчас всё запушено → `git fetch && git reset --hard origin/main` на сервере приведёт состояние в норму.
2. **Два конкурирующих способа деплоя:** `deploy-rsync-from-local.sh` (rsync из локали) и `deploy-pull.sh` + `_tmp-prod-git-reset.sh` (git на сервере). Они портят состояние друг друга; в исключениях `git clean -fd` **нет `.next`** — запуск без пересборки положит сайт. Нужно выбрать один пайплайн.
3. **10 неתracked `_tmp-*` скриптов** — инструменты работ 22–23.07 (DNS-деплой, повторные письма, рестарты, отчёт в Telegram). Секретов внутри нет (проверено). Удалить или узаконить постоянными именами.
4. Мелочи: `/api/health` показывает `d469cdb` при более новом коде (хэш запечён при сборке — исправится следующим деплоем); на проде два git-stash от июня; хвост `/tmp/deploy.log` с 502 — снимок момента рестарта, не текущая проблема.

## Вердикт

Кодовая часть недели в хорошем состоянии: инцидент PayKeeper закрыт системно, SEO-цепочка (robots → sitemap → IndexNow → статьи → перелинковка) работает целиком, sitemap не теряет статьи (26 blog-URL), `prod-verify` — 0 провалов. Требует решения дисциплина деплоя: один пайплайн и правило «сначала коммит — потом прод».

## Открытые ручные шаги владельца (блокируют рост SEO)

- Подтвердить сайт в Google Search Console и Яндекс.Вебмастере, отправить sitemap (см. [SEO.md](SEO.md) §5).
- Реальные телефон/адрес, ИНН и ОГРНИП (подвал, `/contacts`, правовые документы, schema.org).
- Счётчик аналитики (Метрика/GA) — на сайте нет ни одного.
