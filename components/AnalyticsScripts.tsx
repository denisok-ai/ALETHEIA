/**
 * Microsoft Clarity отключён по умолчанию (иностранный сервис, риск трансграничной передачи ПДн).
 * Компонент оставлен для явного опционального включения при отдельном решении.
 */
export function AnalyticsScripts() {
  return null;
}
