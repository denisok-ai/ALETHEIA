# Долгий таймаут при нестабильной сети (скачивание Prisma engines и т.д.)
fetch-timeout=120000
# next-auth@4 объявляет optional peer nodemailer@^7; мы используем nodemailer@10 и
# credentials-провайдер (email-логин не нужен). Без флага npm ci падает на ERESOLVE.
legacy-peer-deps=true
